{
	"extends": "tsconfig/react-library.json",
	"compilerOptions": {
		"baseUrl": "../..",
		"paths": {
			"@config": ["config.ts"]
		}
	},
	"include": ["**/*.ts"],
	"exclude": ["dist", "build", "node_modules"]
}
